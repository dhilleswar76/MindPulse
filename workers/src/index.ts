import dotenv from 'dotenv';
dotenv.config();

console.log('⚡ MindPulse Background Workers Engine Initialized.');
console.log('📦 BullMQ queues configured for: [risk-recomputation, weekly-digest, anomaly-scan]');
console.log('ℹ️ Worker listener standby ready.');
